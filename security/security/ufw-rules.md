## Firewall Rules

### Allowed Ports
The following ports are allowed on the server:

Allowed Ports:
- 22 (SSH) : Secure remote access
- 80 (HTTP) : Web traffic
- 443 (HTTPS) : Secure web traffic

Commands used:
sudo ufw allow 22
sudo ufw allow 80
sudo ufw allow 443
